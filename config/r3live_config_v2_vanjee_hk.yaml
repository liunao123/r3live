Lidar_front_end:
   lidar_type: 2   # 1 for Livox-avia, 3 for Ouster-OS1-64
   N_SCANS: 16
   using_raw_point: 0
   point_step: 2
   blind: 3.0
   
r3live_common:
   if_dump_log: 0                   # If recording ESIKF update log. [default = 0]
   record_offline_map: 1            # If recording offline map. [default = 1]
   pub_pt_minimum_views: 3          # Publish points which have been render up to "pub_pt_minimum_views" time. [default = 3]
   minimum_pts_size: 0.05          # The minimum distance for every two points in Global map (unit in meter). [default = 0.01] 
   image_downsample_ratio: 1         # The downsample ratio of the input image. [default = 1]
   estimate_i2c_extrinsic: 1         # If enable estimate the extrinsic between camera and IMU. [default = 1] 
   estimate_intrinsic: 0            # If enable estimate the online intrinsic calibration of the camera lens. [default = 1] 
   maximum_vio_tracked_pts: 600     # The maximum points for tracking. [default = 600]
   append_global_map_point_step: 4  # The point step of append point to global map. [default = 4]
   control_image_freq: 25.0 # The frequency [default = 10]

r3live_vio:
   image_width: 612
   image_height: 512
   camera_intrinsic:
      [625.42654, 0.0, 306.26709,
      0.0,  625.61129, 251.60909,
      0.0, 0.0, 1.0 ]
   camera_dist_coeffs: [-0.101191 , 0.1449130  , 0.0014190 , -0.000249 , 0]  #k1, k2, p1, p2, k3


   # Fine extrinsic value. form camera-LiDAR calibration.
   # 多次联合标定的均值 用这个
   camera_ext_R:
         [-0.0429599   ,  -0.000655486  ,  0.999077,
          -0.999029   , 0.00980221 ,  -0.0429514 ,
         -0.00976501   , -0.999952 , -0.00107595]
   camera_ext_t: [0.0 , 0.0 , 0.100046]
   
r3live_lio:        
   lio_update_point_step: 4   # Point step used for LIO update.  
   max_iteration: 2           # Maximum times of LIO esikf.
   lidar_time_delay: 0.0        # The time-offset between LiDAR and IMU, provided by user. 
   filter_size_corner: 0.10
   filter_size_surf: 0.10
   filter_size_surf_z: 0.10
   filter_size_map: 0.30
   
   lidar2imu_ext_R:
         [1 , 0 , 0,
          0 , 1 , 0,
          0 , 0 , 1]
   lidar2imu_ext_t: [ -0.0213 , -0.0172, 0.0362 ]